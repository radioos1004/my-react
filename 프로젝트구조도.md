src/
├── assets/          # 이미지, 폰트 등 정적 파일
├── components/      # 재사용 가능한 UI 컴포넌트
├── pages/           # 라우트 단위 페이지 컴포넌트
├── hooks/           # 커스텀 훅
├── services/        # API 통신 로직
├── contexts/        # Context API (전역 상태)
├── utils/           # 유틸리티 함수
├── constants/       # 상수 값
├── styles/          # 전역 스타일
├── routes/          # 라우팅 설정
└── App.jsx          # 앱 진입점



components/
├── common/                    # 공통 컴포넌트 (Atom)
│   ├── Button/
│   │   ├── Button.jsx
│   │   ├── Button.module.css
│   │   └── index.js
│   ├── Input/
│   ├── Modal/
│   └── FileUploader/          # 파일 업로드 전용 컴포넌트
│       ├── FileUploader.jsx   # 숨김 input + 커스텀 버튼 [citation:4]
│       └── index.js
│
├── layout/                    # 레이아웃 컴포넌트 (Molecule) [citation:5]
│   ├── Header/
│   │   ├── Header.jsx
│   │   ├── Logo.jsx
│   │   ├── Navigation.jsx
│   │   └── index.js
│   └── Footer/
│
└── domain/                    # 도메인별 컴포넌트 (Organism)
    ├── auth/
    │   ├── LoginForm.jsx      # 로그인 폼
    │   └── RegisterForm.jsx   # 회원가입 폼
    ├── board/
    │   ├── PostList.jsx       # 게시글 목록
    │   ├── PostItem.jsx       # 게시글 아이템
    │   ├── PostForm.jsx       # 게시글 작성/수정
    │   └── PostDetail.jsx     # 게시글 상세
    ├── comment/
    │   ├── CommentList.jsx    # 댓글 목록
    │   ├── CommentItem.jsx    # 댓글 아이템
    │   └── CommentForm.jsx    # 댓글 입력 폼 [citation:3][citation:7]
    └── reply/
        └── ReplyList.jsx      # 답변 목록 (댓글 구조와 유사) [citation:7]


pages/
├── Home/
│   └── Home.jsx               # 메인 페이지
├── Auth/
│   ├── Login.jsx              # 로그인 페이지
│   └── Register.jsx           # 회원가입 페이지
├── Board/
│   ├── BoardList.jsx          # 게시판 목록 페이지
│   ├── BoardWrite.jsx         # 게시글 작성 페이지
│   ├── BoardDetail.jsx        # 게시글 상세 페이지
│   └── BoardEdit.jsx          # 게시글 수정 페이지
└── MyPage/
    └── MyPage.jsx             # 마이페이지


hooks/
├── useAuth.js                 # 인증 관련 훅 (로그인 상태, 토큰 관리)
├── useForm.js                  # 폼 입력 관리 및 유효성 검사 [citation:2]
├── useFileUpload.js            # 파일 업로드 로직 [citation:4]
├── usePagination.js            # 페이지네이션 로직
├── usePosts.js                  # 게시글 데이터 페칭
└── useComments.js              # 댓글/답변 데이터 관리


services/
├── apiClient.js               # axios 인스턴스 설정 [citation:6]
├── authService.js             # 로그인/회원가입 API
├── boardService.js            # 게시글 CRUD API
├── commentService.js          # 댓글/답변 API
└── fileService.js             # 파일 업로드 API


contexts/
├── AuthContext.jsx            # 인증 상태 전역 관리
└── BoardContext.jsx           # 게시판 상태 관리 (useReducer 활용)


utils/
├── validators.js              # 이메일, 비밀번호 유효성 검사 [citation:2]
├── formatters.js              # 날짜, 파일 크기 포맷팅
├── fileUtils.js               # 파일 처리 유틸리티 [citation:4]
└── constants.js               # 상수 값

constants/
├── index.js                   # 상수 모음
└── fileConstants.js           # 파일 업로드 관련 상수


routes/
├── index.js                   # 라우트 모음
└── PrivateRoute.jsx           # 인증 필요 라우트